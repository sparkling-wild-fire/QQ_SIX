# 为什么包名要那么多级  =》多人分工开发时不至于报名冲突
# select返回的数据形式是怎么样的结果集
# style=wx.DEFAULT_FRAME_STYLE ^ wx.MAXIMIZE_BOX)   ^ ?   =》不用管
# pymysql 连接数据库也是通过socket，在主机名错误的同时会报lib/socket错误
# str类型的127.0.0.1不就是‘127.0.0.1’？
# https://www.cnblogs.com/kunpengv5/p/7811566.html
# python 中对象的类型和值都相同 ，为什么不相等(==)=>可能是全半角问题，但这里应该不是
# 同一个主机发送消息的端口号每次都不一样,不是发消息的每次，是开启客户端的每次
#  # 从服务器端接受数据,阻止程序的继续进行
        json_data,_=client_socket.recvfrom(1024)  # 服务器返回json数据=》是会阻止程序继续往下执行，直到接收到数据？还是说会开启一个子线程？